version: 2

models:
  - name: fct_credit
    columns:
      - name: pk
        tests:
          - unique
          - not_null
  - name: fct_referral
    columns:
      - name: id_tracking_order
        tests:
          - unique
          - not_null
  - name: fct_referrer
    columns:
      - name: referrer_id
        tests:
          - unique
          - not_null